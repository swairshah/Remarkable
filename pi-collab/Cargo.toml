[package]
name = "pi-collab"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"        # SEQPACKET sockets, mmap, poll, sigaction, evdev
serde_json = "1"    # pi's RPC protocol is JSON lines over stdin/stdout

[profile.release]
strip = true
lto = true
opt-level = "s"
