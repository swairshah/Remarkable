[package]
name = "sample-app-rs"
version = "0.1.0"
edition = "2021"

# The only dependency: raw libc bindings for the pieces std doesn't cover
# (SOCK_SEQPACKET sockets, mmap, sigaction).
[dependencies]
libc = "0.2"

[profile.release]
strip = true
lto = true
opt-level = "s"
